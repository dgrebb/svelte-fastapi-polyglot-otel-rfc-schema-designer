# UI

SvelteKit frontend for **svelte-fastapi-remote-functions**. See the [root README](../README.md) for setup and architecture.

```bash
pnpm install
pnpm dev
pnpm check
```

Highlights: `src/routes/status/` (`query.live` live stream), `src/lib/server/status-stream.ts`.
